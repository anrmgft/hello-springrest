# hello-springrest

## Instrucciones

`Dockerfile`

```shell
docker build -t spring:latest .
```
* Run
```shell
docker run -it --rm -p 8080:8080 spring
```

## Instrucciones

`docker-compose`

```shell
docker-compose build
```
* Run
```shell
docker-compose up
```
